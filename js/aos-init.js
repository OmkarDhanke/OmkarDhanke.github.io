/* ============================================================
   AOS INIT
   Scroll animation library initialization.
   ============================================================ */

AOS.init({
  duration: 700,
  once: true,
  offset: 80,
});
